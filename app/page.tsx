import GateExperience from '@/components/GateExperience'

export default function Home() {
  return (
    <main style={{ minHeight: '100vh', position: 'relative' }}>
      <GateExperience />
    </main>
  )
}
